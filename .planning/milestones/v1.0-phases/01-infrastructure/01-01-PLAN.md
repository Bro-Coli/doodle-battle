---
phase: 01-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pnpm-workspace.yaml
  - package.json
  - tsconfig.base.json
  - .env
  - .env.example
  - .gitignore
  - shared/package.json
  - shared/src/types.ts
  - server/package.json
  - server/tsconfig.json
  - server/src/index.ts
  - server/src/routes/recognize.ts
  - server/src/mock-entities.ts
  - server/vitest.config.ts
  - server/tests/mock.test.ts
autonomous: true
requirements:
  - INFR-01
  - INFR-02

must_haves:
  truths:
    - "POST /api/recognize returns a valid EntityProfile JSON when MOCK_AI=true"
    - "Mock mode auto-activates when ANTHROPIC_API_KEY is missing from env"
    - "ANTHROPIC_API_KEY is loaded only on the server process, never exposed to client"
    - "pnpm install from root resolves all three workspace packages without errors"
    - "shared types are importable from both client and server packages via @crayon-world/shared"
  artifacts:
    - path: "shared/src/types.ts"
      provides: "EntityProfile, Archetype type definitions"
      exports: ["EntityProfile", "Archetype"]
    - path: "server/src/routes/recognize.ts"
      provides: "POST /api/recognize endpoint"
      exports: ["default"]
    - path: "server/src/mock-entities.ts"
      provides: "6 hardcoded EntityProfile objects"
      exports: ["MOCK_ENTITIES"]
    - path: "pnpm-workspace.yaml"
      provides: "Workspace package declarations"
      contains: "packages"
  key_links:
    - from: "server/src/routes/recognize.ts"
      to: "server/src/mock-entities.ts"
      via: "import MOCK_ENTITIES"
      pattern: "import.*MOCK_ENTITIES.*mock-entities"
    - from: "server/src/mock-entities.ts"
      to: "shared/src/types.ts"
      via: "import type EntityProfile"
      pattern: "import.*EntityProfile.*@crayon-world/shared"
    - from: "server/src/index.ts"
      to: "server/src/routes/recognize.ts"
      via: "app.use('/api/recognize', recognizeRouter)"
      pattern: "app\\.use.*api/recognize"
---

<objective>
Scaffold the pnpm monorepo with shared TypeScript types, Express API proxy server, and mock AI mode.

Purpose: Establish the project foundation — workspace structure, shared type contracts, and a working API endpoint that returns mock entity profiles without any Anthropic API key required.
Output: Working monorepo with `pnpm install` succeeding, server running via `tsx watch`, POST /api/recognize returning mock EntityProfile JSON.
</objective>

<execution_context>
@/Users/axelpothier/.claude/get-shit-done/workflows/execute-plan.md
@/Users/axelpothier/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold monorepo with shared types</name>
  <files>
    pnpm-workspace.yaml,
    package.json,
    tsconfig.base.json,
    .env,
    .env.example,
    .gitignore,
    shared/package.json,
    shared/src/types.ts,
    server/package.json,
    server/tsconfig.json,
    client/package.json,
    client/tsconfig.json
  </files>
  <action>
Create the full monorepo scaffold from scratch. This is a greenfield project with only a .planning/ directory and a PDF.

1. Create root `pnpm-workspace.yaml` listing client, server, shared packages.

2. Create root `package.json` with name "@crayon-world/root", private: true, scripts:
   - "dev:client": "pnpm --filter @crayon-world/client dev"
   - "dev:server": "pnpm --filter @crayon-world/server dev"
   - "test": "pnpm -r test --run"

3. Create root `tsconfig.base.json` with strict: true, esModuleInterop: true, skipLibCheck: true, moduleResolution: "bundler", target: "ES2022", module: "ESNext".

4. Create `.env` with:
   ```
   # ANTHROPIC_API_KEY=sk-ant-xxxxx
   MOCK_AI=true
   PORT=3001
   ```

5. Create `.env.example` with same structure but no values (just variable names and comments).

6. Create `.gitignore` including: node_modules, dist, .env, *.log, .DS_Store.

7. Create `shared/package.json` with name "@crayon-world/shared", main: "./src/types.ts", types: "./src/types.ts". No build step.

8. Create `shared/src/types.ts` with the Archetype union type and EntityProfile interface exactly as specified in the research:
   - Archetype = 'walking' | 'flying' | 'rooted' | 'spreading' | 'drifting' | 'stationary'
   - EntityProfile = { name: string, archetype: Archetype, traits: string[], role: string }

9. Create `server/package.json` with name "@crayon-world/server", type: "module", scripts: { "dev": "tsx watch src/index.ts", "test": "vitest run" }, dependencies: { express, cors, dotenv, "@crayon-world/shared": "workspace:*" }, devDependencies: { tsx, typescript, "@types/express": "^5", "@types/cors", "@types/node", vitest }.

10. Create `server/tsconfig.json` extending ../tsconfig.base.json with outDir: "./dist", rootDir: "./src".

11. Create `client/package.json` with name "@crayon-world/client", type: "module", scripts: { "dev": "vite", "build": "vite build" }, dependencies: { "pixi.js": "^8.16.0", "@crayon-world/shared": "workspace:*" }, devDependencies: { vite, typescript }.

12. Create `client/tsconfig.json` extending ../tsconfig.base.json with outDir: "./dist", rootDir: "./src".

13. Run `pnpm install` from repo root.

Verify workspace resolution works: `pnpm ls -r --depth 0` should show all three packages.
  </action>
  <verify>
    <automated>cd "/Users/axelpothier/Documents/Code/GameDev/Drawing Game" && pnpm install --frozen-lockfile 2>/dev/null || pnpm install && pnpm ls -r --depth 0</automated>
  </verify>
  <done>All three workspace packages resolve, pnpm install succeeds, shared/src/types.ts exports EntityProfile and Archetype types</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Express server with mock AI endpoint</name>
  <files>
    server/src/index.ts,
    server/src/routes/recognize.ts,
    server/src/mock-entities.ts,
    server/vitest.config.ts,
    server/tests/mock.test.ts
  </files>
  <behavior>
    - Test 1: MOCK_ENTITIES array contains exactly 6 entities, one per archetype
    - Test 2: Each mock entity has all required EntityProfile fields (name, archetype, traits, role)
    - Test 3: isMockMode returns true when ANTHROPIC_API_KEY is undefined
    - Test 4: isMockMode returns true when MOCK_AI is "true" (even if key exists)
    - Test 5: isMockMode returns false when MOCK_AI is not "true" and ANTHROPIC_API_KEY is set
  </behavior>
  <action>
1. Create `server/vitest.config.ts` with basic config (no special settings needed).

2. Create `server/src/mock-entities.ts` exporting MOCK_ENTITIES array with 6 EntityProfile objects:
   - Wolf (walking), Eagle (flying), Oak (rooted), Fire (spreading), Cloud (drifting), Rock (stationary)
   Each with traits array and role string as specified in research.

3. Create `server/src/routes/recognize.ts`:
   - Export a function `isMockMode()` that checks `process.env.MOCK_AI === 'true' || !process.env.ANTHROPIC_API_KEY`
   - Export isMockMode for testing (named export alongside default router)
   - Router with POST '/' handler that:
     - If mock mode: returns random entity from MOCK_ENTITIES with status 200
     - If not mock mode: returns 501 with { error: "Real AI not implemented yet" }
   - Import EntityProfile type from @crayon-world/shared

4. Create `server/src/index.ts`:
   - import 'dotenv/config' first
   - Import express, cors, recognizeRouter
   - Configure cors({ origin: 'http://localhost:5173' })
   - app.use(express.json())
   - app.use('/api/recognize', recognizeRouter)
   - Listen on PORT from env (default 3001)
   - Log mock mode status on startup

5. Create `server/tests/mock.test.ts` with the behavior tests listed above.
   - Import MOCK_ENTITIES from '../src/mock-entities'
   - Import isMockMode from '../src/routes/recognize'
   - For env var tests: save/restore process.env in beforeEach/afterEach

6. Run tests: `pnpm --filter @crayon-world/server test`
  </action>
  <verify>
    <automated>cd "/Users/axelpothier/Documents/Code/GameDev/Drawing Game" && pnpm --filter @crayon-world/server test</automated>
  </verify>
  <done>All 5 mock tests pass. Server starts with `pnpm dev:server` and POST to localhost:3001/api/recognize returns a valid EntityProfile JSON body.</done>
</task>

</tasks>

<verification>
1. `pnpm install` succeeds at repo root with no errors
2. `pnpm --filter @crayon-world/server test` passes all mock entity tests
3. Start server with `pnpm dev:server`, then `curl -X POST http://localhost:3001/api/recognize` returns valid JSON with name, archetype, traits, role fields
4. `grep -r "ANTHROPIC_API_KEY" client/` returns no matches (key not leaked to client)
</verification>

<success_criteria>
- Monorepo scaffold complete with pnpm workspaces resolving all three packages
- Shared types (EntityProfile, Archetype) importable from @crayon-world/shared in both client and server
- POST /api/recognize returns a random mock EntityProfile when MOCK_AI=true or API key is absent
- Server runs via `tsx watch` with hot reload
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure/01-01-SUMMARY.md`
</output>
