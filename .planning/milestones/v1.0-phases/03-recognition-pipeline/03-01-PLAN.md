---
phase: 03-recognition-pipeline
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/recognition/validateProfile.ts
  - server/src/recognition/buildPrompt.ts
  - server/src/recognition/anthropicClient.ts
  - server/src/routes/recognize.ts
  - server/tests/validateProfile.test.ts
  - server/tests/recognize.test.ts
  - server/package.json
autonomous: true
requirements: [RECG-01, RECG-02, RECG-04]

must_haves:
  truths:
    - "POST /api/recognize with imageDataUrl returns a valid EntityProfile in mock mode"
    - "POST /api/recognize with imageDataUrl calls Claude Haiku and returns a validated EntityProfile in real mode"
    - "Malformed JSON from Claude returns a Mystery Blob fallback entity"
    - "Unknown archetypes from Claude map to stationary"
    - "Recognized entity names are cached — second call with same name skips Claude"
  artifacts:
    - path: "server/src/recognition/validateProfile.ts"
      provides: "EntityProfile type guard with unknown-archetype fallback"
      exports: ["validateEntityProfile", "mysteryBlob"]
    - path: "server/src/recognition/buildPrompt.ts"
      provides: "System prompt and user message construction"
      exports: ["SYSTEM_PROMPT", "buildUserContent"]
    - path: "server/src/recognition/anthropicClient.ts"
      provides: "Lazy-init Anthropic SDK singleton"
      exports: ["getAnthropicClient"]
    - path: "server/src/routes/recognize.ts"
      provides: "POST route with mock mode, cache, real Anthropic call, validation"
      exports: ["default", "isMockMode"]
    - path: "server/tests/validateProfile.test.ts"
      provides: "Unit tests for validation guard"
    - path: "server/tests/recognize.test.ts"
      provides: "Integration tests for recognize route with mocked Anthropic SDK"
  key_links:
    - from: "server/src/routes/recognize.ts"
      to: "server/src/recognition/anthropicClient.ts"
      via: "getAnthropicClient() call"
      pattern: "getAnthropicClient\\(\\)"
    - from: "server/src/routes/recognize.ts"
      to: "server/src/recognition/validateProfile.ts"
      via: "validateEntityProfile(parsed)"
      pattern: "validateEntityProfile\\("
    - from: "server/src/routes/recognize.ts"
      to: "server/src/recognition/buildPrompt.ts"
      via: "SYSTEM_PROMPT and buildUserContent imports"
      pattern: "SYSTEM_PROMPT|buildUserContent"
---

<objective>
Server-side recognition pipeline: install Anthropic SDK, implement Claude Haiku vision call, validate response into EntityProfile, cache by entity name, return Mystery Blob on failure.

Purpose: The server endpoint is the foundation — the client (Plan 02) POSTs to it and expects a typed EntityProfile back. All validation, fallback, and caching logic lives here.
Output: Working POST /api/recognize that returns EntityProfile in both mock and real modes, with full test coverage for validation logic.
</objective>

<execution_context>
@/Users/axelpothier/.claude/get-shit-done/workflows/execute-plan.md
@/Users/axelpothier/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-recognition-pipeline/03-CONTEXT.md
@.planning/phases/03-recognition-pipeline/03-RESEARCH.md

<interfaces>
<!-- Existing types from shared package — these are the contracts -->
From shared/src/types.ts:
```typescript
export type Archetype =
  | 'walking'
  | 'flying'
  | 'rooted'
  | 'spreading'
  | 'drifting'
  | 'stationary';

export interface EntityProfile {
  name: string;
  archetype: Archetype;
  traits: string[];
  role: string;
}
```

From server/src/routes/recognize.ts (current state — will be expanded):
```typescript
export function isMockMode(): boolean {
  return process.env.MOCK_AI === 'true' || !process.env.ANTHROPIC_API_KEY;
}
// Currently returns 501 for non-mock mode — this plan replaces with real Anthropic call
```

From server/src/mock-entities.ts:
```typescript
export const MOCK_ENTITIES: EntityProfile[];
// 6 entities covering all archetypes: Wolf, Eagle, Oak, Fire, Cloud, Rock
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Validation guard, prompt builder, and Anthropic client modules with tests</name>
  <files>
    server/src/recognition/validateProfile.ts,
    server/src/recognition/buildPrompt.ts,
    server/src/recognition/anthropicClient.ts,
    server/tests/validateProfile.test.ts
  </files>
  <behavior>
    - validateEntityProfile({name:"Wolf",archetype:"walking",traits:["predatory"],role:"Apex predator"}) returns valid EntityProfile
    - validateEntityProfile({name:"Wolf",archetype:"unknown_type",traits:["x"],role:"y"}) returns profile with archetype "stationary"
    - validateEntityProfile(null) returns null
    - validateEntityProfile({}) returns null (missing required fields)
    - validateEntityProfile({name:"",archetype:"walking",traits:[],role:""}) returns null (empty name)
    - validateEntityProfile({name:"Wolf",archetype:"walking",traits:[],role:"x"}) returns null (empty traits)
    - mysteryBlob() returns an EntityProfile with name "Mystery Blob" and a random archetype
    - SYSTEM_PROMPT contains JSON schema with all 4 EntityProfile fields
    - buildUserContent(base64String) returns array with image block and text block
  </behavior>
  <action>
    1. Create `server/src/recognition/validateProfile.ts`:
       - Export `validateEntityProfile(raw: unknown): EntityProfile | null` — pure type guard. Check: raw is object, name is non-empty string, traits is non-empty array of strings, role is non-empty string. Unknown archetype maps to "stationary" per locked decision.
       - Export `mysteryBlob(): EntityProfile` — returns `{ name: "Mystery Blob", archetype: randomArchetype, traits: ["mysterious", "amorphous"], role: "An unidentifiable entity" }` where randomArchetype picks from all 6 archetypes.
       - Import `Archetype` and `EntityProfile` from `@crayon-world/shared`.

    2. Create `server/src/recognition/buildPrompt.ts`:
       - Export `SYSTEM_PROMPT: string` — strict JSON-only prompt telling Claude to classify a freehand sketch into EntityProfile schema (open vocabulary, no refusals, JSON only). See RESEARCH.md Pattern 2 for candidate wording.
       - Export `buildUserContent(base64: string)` — returns the `content` array for the user message: image block (type: "image", source: base64, media_type "image/png") + text block ("Identify this sketch.").

    3. Create `server/src/recognition/anthropicClient.ts`:
       - Export `getAnthropicClient(): Anthropic` — lazy-initialized singleton with `timeout: 30_000` (hard timeout per locked decision). Uses `process.env.ANTHROPIC_API_KEY`.

    4. Create `server/tests/validateProfile.test.ts`:
       - Test all behavior cases listed above. Use `vi.spyOn(Math, 'random')` to test mysteryBlob archetype selection deterministically.

    5. Install SDK: `pnpm --filter @crayon-world/server add @anthropic-ai/sdk`
  </action>
  <verify>
    <automated>cd "/Users/axelpothier/Documents/Code/GameDev/Drawing Game" && pnpm --filter @crayon-world/server test -- --run</automated>
  </verify>
  <done>validateProfile.test.ts passes all branches. validateEntityProfile handles valid, invalid, unknown-archetype, null, and empty-field cases. mysteryBlob returns valid EntityProfile. SYSTEM_PROMPT and buildUserContent produce correct message structure. Anthropic SDK installed in server package.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire recognize route with Anthropic call, cache, validation, and integration tests</name>
  <files>
    server/src/routes/recognize.ts,
    server/tests/recognize.test.ts
  </files>
  <behavior>
    - POST /api/recognize with mock mode returns a random MOCK_ENTITIES profile (existing behavior preserved)
    - POST /api/recognize without imageDataUrl returns 400
    - POST /api/recognize in real mode calls Claude Haiku with the base64 image, validates response, returns EntityProfile
    - POST /api/recognize caches by entity name — second call with same name returns cached profile without calling Claude again
    - POST /api/recognize returns Mystery Blob when Claude returns malformed JSON
    - POST /api/recognize returns 502 when Anthropic SDK throws
  </behavior>
  <action>
    1. Expand `server/src/routes/recognize.ts`:
       - Keep existing mock mode branch (returns random MOCK_ENTITIES).
       - Add input validation: require `imageDataUrl` as non-empty string in req.body, return 400 if missing.
       - Strip `data:image/png;base64,` prefix from imageDataUrl.
       - Call `getAnthropicClient().messages.create()` with model `claude-haiku-4-5`, max_tokens 256, SYSTEM_PROMPT, and buildUserContent(base64).
       - Extract text from response: `response.content[0]?.type === 'text' ? response.content[0].text : ''`.
       - Try JSON.parse with `{...}` regex extraction fallback (see RESEARCH.md Pattern for pitfall 4).
       - Pass parsed result through `validateEntityProfile()`. If null, use `mysteryBlob()`.
       - Cache: module-scope `Map<string, EntityProfile>`. After successful recognition, store by `profile.name.toLowerCase()`. Before calling Claude, there is no pre-call cache check (name unknown until Claude responds). After getting a profile, check if a profile with the same name is already cached and return that instead of the new one (for consistency).
       - On Anthropic SDK error (catch block): return 502 with `{ error: "Recognition failed" }`.
       - Make the route handler `async`.

    2. Create `server/tests/recognize.test.ts`:
       - Mock `@anthropic-ai/sdk` using `vi.mock()` to avoid real API calls.
       - Test mock mode returns valid EntityProfile (set MOCK_AI=true in env).
       - Test real mode: mock Anthropic to return valid JSON text block, verify route returns validated EntityProfile.
       - Test malformed JSON from Claude: mock Anthropic to return "I can't identify this", verify Mystery Blob returned.
       - Test missing imageDataUrl returns 400.
       - Test Anthropic SDK throwing returns 502.
       - Test cache: call twice with different images but mock Claude to return same entity name — second call should still work (cache stores after first call).
       - Use supertest or direct router invocation for HTTP testing. If supertest is not installed, test the route handler logic by importing it and creating mock req/res objects.
  </action>
  <verify>
    <automated>cd "/Users/axelpothier/Documents/Code/GameDev/Drawing Game" && pnpm --filter @crayon-world/server test -- --run</automated>
  </verify>
  <done>All server tests pass. POST /api/recognize handles: mock mode (random entity), real mode (Claude Haiku call + validation), malformed JSON (Mystery Blob fallback), missing input (400), API failure (502), and entity name caching. The 501 placeholder is replaced with a working pipeline.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @crayon-world/server test -- --run` — all tests pass (existing mock.test.ts + new validateProfile.test.ts + recognize.test.ts)
- `pnpm --filter @crayon-world/server run build` or `npx tsc --noEmit -p server/tsconfig.json` — TypeScript compiles clean
- Manual: `MOCK_AI=true pnpm run dev:server` then `curl -X POST http://localhost:3001/api/recognize -H "Content-Type: application/json" -d '{"imageDataUrl":"data:image/png;base64,iVBOR"}' ` returns a valid EntityProfile JSON
</verification>

<success_criteria>
- Server tests green with full branch coverage of validateEntityProfile
- POST /api/recognize returns EntityProfile in mock mode (no regression)
- POST /api/recognize returns EntityProfile via Claude Haiku in real mode (when API key set)
- Malformed Claude responses produce Mystery Blob, not 500
- Unknown archetypes silently map to "stationary"
- Entity name cache prevents duplicate Claude calls within a session
</success_criteria>

<output>
After completion, create `.planning/phases/03-recognition-pipeline/03-01-SUMMARY.md`
</output>
