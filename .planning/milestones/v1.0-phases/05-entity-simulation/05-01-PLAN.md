---
phase: 05-entity-simulation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/src/types.ts
  - server/src/recognition/buildPrompt.ts
  - server/src/recognition/validateProfile.ts
  - server/src/mock-entities.ts
  - server/tests/validateProfile.test.ts
  - server/tests/mock.test.ts
autonomous: true
requirements: [ENTY-02]

must_haves:
  truths:
    - "EntityProfile includes a speed field (number 1-10)"
    - "Server validation clamps or defaults speed to 5 when invalid"
    - "Claude Haiku prompt asks for a speed value in the JSON schema"
    - "All 6 mock entities have speed values appropriate to their identity"
  artifacts:
    - path: "shared/src/types.ts"
      provides: "EntityProfile with speed: number field"
      contains: "speed: number"
    - path: "server/src/recognition/validateProfile.ts"
      provides: "speed validation (1-10, default 5)"
      contains: "speed"
    - path: "server/src/recognition/buildPrompt.ts"
      provides: "Claude prompt requesting speed field"
      contains: "speed"
    - path: "server/src/mock-entities.ts"
      provides: "6 mock entities with speed values"
      contains: "speed"
  key_links:
    - from: "shared/src/types.ts"
      to: "server/src/recognition/validateProfile.ts"
      via: "EntityProfile import"
      pattern: "import.*EntityProfile"
    - from: "server/src/recognition/validateProfile.ts"
      to: "shared/src/types.ts"
      via: "returns EntityProfile with speed"
      pattern: "speed"
---

<objective>
Extend the EntityProfile type with a `speed` field and propagate it through the entire server pipeline: shared type definition, Claude Haiku prompt, server-side validation, and mock entity data.

Purpose: The simulation engine (Plan 02) needs a speed value per entity to compute pixels/second movement rates. This plan ensures every entity — whether from Claude or mock data — arrives at the client with a valid speed number.

Output: Updated shared type, prompt, validation, mock data, and extended test coverage.
</objective>

<execution_context>
@/Users/axelpothier/.claude/get-shit-done/workflows/execute-plan.md
@/Users/axelpothier/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-entity-simulation/05-CONTEXT.md
@.planning/phases/05-entity-simulation/05-RESEARCH.md

<interfaces>
<!-- Existing types and functions to modify -->

From shared/src/types.ts:
```typescript
export type Archetype = 'walking' | 'flying' | 'rooted' | 'spreading' | 'drifting' | 'stationary';

export interface EntityProfile {
  name: string;
  archetype: Archetype;
  traits: string[];
  role: string;
  // ADD: speed: number;  // 1-10 scale
}
```

From server/src/recognition/validateProfile.ts:
```typescript
export function validateEntityProfile(raw: unknown): EntityProfile | null;
export function mysteryBlob(): EntityProfile;
```

From server/src/recognition/buildPrompt.ts:
```typescript
export const SYSTEM_PROMPT: string;
export function buildUserContent(base64: string): Array<{...}>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add speed field to EntityProfile and update server pipeline</name>
  <files>shared/src/types.ts, server/src/recognition/buildPrompt.ts, server/src/recognition/validateProfile.ts, server/src/mock-entities.ts</files>
  <behavior>
    - EntityProfile has `speed: number` field
    - validateEntityProfile: valid speed (number 1-10) passes through rounded to integer
    - validateEntityProfile: missing speed defaults to 5
    - validateEntityProfile: out-of-range speed (0, 11, -1, 100) clamps to 1-10
    - validateEntityProfile: non-number speed (string, null) defaults to 5
    - mysteryBlob() returns speed: 5
    - SYSTEM_PROMPT includes speed in the JSON schema with description
    - All 6 mock entities have speed values: Wolf=7, Eagle=8, Oak=1, Fire=6, Cloud=3, Rock=1
  </behavior>
  <action>
1. **shared/src/types.ts**: Add `speed: number; // 1-10 scale from Claude, maps to pixels/second in simulation` to EntityProfile interface.

2. **server/src/recognition/buildPrompt.ts**: Add `"speed": number` to the JSON schema in SYSTEM_PROMPT with comment `// 1-10 how fast this entity moves in real life (1=snail, 10=cheetah)`.

3. **server/src/recognition/validateProfile.ts**:
   - In `validateEntityProfile()`, after archetype handling, add speed extraction:
     ```
     const rawSpeed = obj['speed'];
     const speed = typeof rawSpeed === 'number'
       ? Math.round(Math.max(1, Math.min(10, rawSpeed)))
       : 5;
     ```
   - Include `speed` in the returned EntityProfile object.
   - In `mysteryBlob()`, add `speed: 5` to the returned object.

4. **server/src/mock-entities.ts**: Add speed to each mock entity:
   - Wolf: 7 (fast predator)
   - Eagle: 8 (swift flyer)
   - Oak: 1 (immobile)
   - Fire: 6 (medium-fast spread)
   - Cloud: 3 (slow drifter)
   - Rock: 1 (immobile)
  </action>
  <verify>
    <automated>cd "/Users/axelpothier/Documents/Code/GameDev/Drawing Game" && pnpm run test -- --reporter=verbose</automated>
  </verify>
  <done>EntityProfile has speed field, validation handles all edge cases, prompt asks for speed, all mock entities have appropriate speed values, all existing tests still pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend tests for speed validation and mock entity speed</name>
  <files>server/tests/validateProfile.test.ts, server/tests/mock.test.ts</files>
  <behavior>
    - Test: valid profile with speed 7 returns speed 7
    - Test: missing speed field defaults to 5
    - Test: speed as string defaults to 5
    - Test: speed 0 clamps to 1
    - Test: speed 11 clamps to 10
    - Test: speed 5.7 rounds to 6
    - Test: mysteryBlob returns speed 5
    - Test: all 6 mock entities have speed in [1, 10]
  </behavior>
  <action>
1. **server/tests/validateProfile.test.ts**: Add a new `describe('speed validation')` block inside the existing `validateEntityProfile` describe:
   - `it('includes speed when valid')` — input speed: 7, expect result.speed === 7
   - `it('defaults speed to 5 when missing')` — omit speed field, expect result.speed === 5
   - `it('defaults speed to 5 when non-number')` — speed: "fast", expect 5
   - `it('clamps speed below 1 to 1')` — speed: 0, expect 1; speed: -5, expect 1
   - `it('clamps speed above 10 to 10')` — speed: 11, expect 10; speed: 100, expect 10
   - `it('rounds fractional speed')` — speed: 5.7, expect 6; speed: 3.2, expect 3
   - Update the existing `'returns valid EntityProfile for well-formed input'` test to include speed in input and expectation.

   In the `mysteryBlob` describe:
   - `it('returns speed 5')` — expect mysteryBlob().speed === 5
   - Update existing `'returns a valid EntityProfile structure'` test to check `typeof blob.speed === 'number'`.

2. **server/tests/mock.test.ts**: Inside the existing `MOCK_ENTITIES` describe:
   - `it('each entity has a valid speed between 1 and 10')` — loop all, expect speed >= 1 && speed <= 10 && Number.isInteger(speed)
  </action>
  <verify>
    <automated>cd "/Users/axelpothier/Documents/Code/GameDev/Drawing Game" && pnpm run test -- --reporter=verbose</automated>
  </verify>
  <done>All new speed tests pass alongside all existing tests. Coverage: valid speed, missing speed, out-of-range, non-number, fractional, mysteryBlob speed, mock entity speeds.</done>
</task>

</tasks>

<verification>
- `pnpm run test` passes with all new and existing tests green
- `shared/src/types.ts` exports EntityProfile with speed: number
- `server/src/recognition/buildPrompt.ts` SYSTEM_PROMPT contains "speed"
- `server/src/recognition/validateProfile.ts` handles speed validation
- All 6 mock entities have integer speed values in [1, 10]
</verification>

<success_criteria>
- EntityProfile type includes speed: number field
- Claude prompt requests speed in JSON schema
- Validation clamps/defaults speed correctly
- Mock entities have identity-appropriate speed values
- All tests pass (existing + new speed tests)
</success_criteria>

<output>
After completion, create `.planning/phases/05-entity-simulation/05-01-SUMMARY.md`
</output>
