---
phase: 06-shared-types-interaction-server-route
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - server/src/routes/interactions.ts
  - server/src/index.ts
  - server/tests/interactions.test.ts
autonomous: true
requirements: [BATC-01, BATC-02, BATC-03]

must_haves:
  truths:
    - "POST /api/interactions accepts an array of entity profiles and returns a relationship matrix"
    - "Mock mode returns canned relationships without calling the Anthropic API"
    - "Single-entity request returns empty entries array without calling AI"
    - "Invalid/malformed AI response retries once then falls back to all-ignore"
    - "Route deduplicates by entity name before prompting AI"
    - "Tests cover mock mode, real mode with mocked SDK, input validation, edge cases"
  artifacts:
    - path: "server/src/routes/interactions.ts"
      provides: "POST /api/interactions route handler"
      min_lines: 50
    - path: "server/src/index.ts"
      provides: "Route registration for /api/interactions"
      contains: "interactionsRouter"
    - path: "server/tests/interactions.test.ts"
      provides: "Test suite for interaction route"
      min_lines: 80
  key_links:
    - from: "server/src/routes/interactions.ts"
      to: "server/src/interaction/buildInteractionPrompt.ts"
      via: "import INTERACTION_SYSTEM_PROMPT, buildInteractionUserContent"
      pattern: "import.*buildInteractionUserContent.*from.*interaction/buildInteractionPrompt"
    - from: "server/src/routes/interactions.ts"
      to: "server/src/interaction/validateInteraction.ts"
      via: "import validateInteractionResponse, ignoreFallback"
      pattern: "import.*validateInteractionResponse.*from.*interaction/validateInteraction"
    - from: "server/src/routes/interactions.ts"
      to: "server/src/mock-interactions.ts"
      via: "import MOCK_INTERACTION_MATRIX"
      pattern: "import.*MOCK_INTERACTION_MATRIX.*from.*mock-interactions"
    - from: "server/src/routes/interactions.ts"
      to: "server/src/routes/recognize.ts"
      via: "import isMockMode"
      pattern: "import.*isMockMode.*from.*recognize"
    - from: "server/src/index.ts"
      to: "server/src/routes/interactions.ts"
      via: "app.use('/api/interactions', interactionsRouter)"
      pattern: "app\\.use.*api/interactions"
---

<objective>
Create the POST /api/interactions route, register it in the Express app, and write a comprehensive test suite covering mock mode, real AI mode, input validation, and edge cases.

Purpose: Complete the interaction API contract so downstream phases (7, 9) can call it from the client during rounds.
Output: Working route at POST /api/interactions, registered in server, with passing test suite.
</objective>

<execution_context>
@/Users/axelpothier/.claude/get-shit-done/workflows/execute-plan.md
@/Users/axelpothier/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-shared-types-interaction-server-route/06-CONTEXT.md
@.planning/phases/06-shared-types-interaction-server-route/06-RESEARCH.md
@.planning/phases/06-shared-types-interaction-server-route/06-01-SUMMARY.md

<interfaces>
<!-- From Plan 06-01 outputs -->

From shared/src/types.ts (new types):
```typescript
export type InteractionType = 'chase' | 'flee' | 'fight' | 'befriend' | 'ignore';

export interface EntityRelationship {
  entityId: string;
  relationships: Record<string, InteractionType>;
}

export interface InteractionMatrix {
  entries: EntityRelationship[];
}
```

From server/src/interaction/buildInteractionPrompt.ts:
```typescript
export const INTERACTION_SYSTEM_PROMPT: string;
export function buildInteractionUserContent(entities: Array<{ id: number; name: string; role: string }>): string;
```

From server/src/interaction/validateInteraction.ts:
```typescript
export function validateInteractionResponse(rawText: string, expectedCount: number): InteractionMatrix | null;
export function ignoreFallback(entities: Array<{ id: number }>): InteractionMatrix;
```

From server/src/mock-interactions.ts:
```typescript
export const MOCK_INTERACTION_MATRIX: InteractionMatrix;
```

From server/src/routes/recognize.ts:
```typescript
export function isMockMode(): boolean;
```

From server/src/recognition/anthropicClient.ts:
```typescript
export function getAnthropicClient(): Anthropic;
```

<!-- Existing test pattern from server/tests/recognize.test.ts -->
Pattern: vi.mock('@anthropic-ai/sdk'), buildApp() with env vars + vi.resetModules(), startServer/stopServer/post helpers using node:http.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create interaction route and register in Express app</name>
  <files>server/src/routes/interactions.ts, server/src/index.ts, server/tests/interactions.test.ts</files>
  <behavior>
    - POST with empty body or missing entities array returns 400
    - POST with entities=[] returns 400
    - POST with 1 entity returns { entries: [] } (no AI call)
    - POST in mock mode returns MOCK_INTERACTION_MATRIX without calling Anthropic
    - POST in real mode with valid entities calls Anthropic once and returns validated InteractionMatrix
    - POST in real mode with malformed AI response retries once then returns all-ignore fallback
    - POST when Anthropic SDK throws returns 502
    - Duplicate entity names are deduplicated before prompting (only unique names sent to AI)
  </behavior>
  <action>
1. Create `server/tests/interactions.test.ts` first (RED phase):
   - Mirror the test structure from `server/tests/recognize.test.ts` exactly: vi.mock SDK, buildApp helper, startServer/stopServer/post helpers
   - buildApp: sets env vars, calls vi.resetModules(), dynamically imports interactions router, mounts at `/api/interactions`
   - post helper: POSTs to `/api/interactions` with JSON body
   - Test cases:
     a. "returns 400 when entities is missing" — POST {} -> 400
     b. "returns 400 when entities is empty array" — POST { entities: [] } -> 400
     c. "returns empty entries for single entity" — POST { entities: [profile] } -> { entries: [] }
     d. "returns mock matrix when MOCK_AI=true" — verify response has entries array with entityId strings
     e. "calls Claude and returns validated matrix in real mode" — mock SDK to return valid JSON array with correct structure, verify response is InteractionMatrix
     f. "retries once on malformed response then falls back to ignore" — mock SDK to return garbage twice, verify all relationships are 'ignore'
     g. "returns 502 when Anthropic SDK throws" — mock SDK to reject
     h. "deduplicates entities by name" — send two entities with same name, verify SDK was called with only 1 unique entity in the prompt

2. Create `server/src/routes/interactions.ts` (GREEN phase):
   - Import Router from express, EntityProfile from @crayon-world/shared
   - Import isMockMode from './recognize.js'
   - Import getAnthropicClient from '../recognition/anthropicClient.js'
   - Import buildInteractionUserContent, INTERACTION_SYSTEM_PROMPT from '../interaction/buildInteractionPrompt.js'
   - Import validateInteractionResponse, ignoreFallback from '../interaction/validateInteraction.js'
   - Import MOCK_INTERACTION_MATRIX from '../mock-interactions.js'
   - Route logic (mirrors recognize.ts structure):
     a. Input validation: if entities is not array or length === 0, return 400
     b. 1-entity edge case: return { entries: [] }
     c. Mock mode: return MOCK_INTERACTION_MATRIX
     d. Deduplicate by name using Set on entity names, build promptEntities array with sequential integer IDs
     e. Log warning if >10 unique entities
     f. Retry loop (max 2 attempts): call client.messages.create with model 'claude-haiku-4-5', max_tokens 1024, INTERACTION_SYSTEM_PROMPT as system, buildInteractionUserContent as user message. Extract text from response. Validate with validateInteractionResponse. If valid, return it.
     g. If both attempts fail validation: return ignoreFallback(promptEntities)
     h. Catch block: log error, return 502
   - Export default router

3. Register route in `server/src/index.ts` (GREEN phase):
   - Add `import interactionsRouter from './routes/interactions.js';`
   - Add `app.use('/api/interactions', interactionsRouter);` after the recognize route

4. Run tests (VERIFY phase) — all 8 tests should pass.
  </action>
  <verify>
    <automated>cd "/Users/axelpothier/Documents/Code/GameDev/Drawing Game" && pnpm --filter @crayon-world/server test -- interactions 2>&1 | tail -30</automated>
  </verify>
  <done>POST /api/interactions route is registered, handles mock mode, real AI mode with retry, input validation, 1-entity edge case, and deduplication. All tests pass. Existing recognize tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Verify full test suite and end-to-end mock curl</name>
  <files></files>
  <action>
1. Run the FULL server test suite (not just interactions) to confirm no regressions in recognize tests.
2. Start the dev server with MOCK_AI=true and curl the new endpoint:
   ```
   curl -X POST http://localhost:3001/api/interactions \
     -H "Content-Type: application/json" \
     -d '{"entities": [{"name":"Wolf","archetype":"walking","traits":["predatory"],"role":"Apex predator","speed":7},{"name":"Eagle","archetype":"flying","traits":["sharp vision"],"role":"Aerial hunter","speed":8}]}'
   ```
   Verify the response is valid JSON with an `entries` array containing EntityRelationship objects.
3. Test edge cases via curl: empty entities array (expect 400), single entity (expect empty entries).
  </action>
  <verify>
    <automated>cd "/Users/axelpothier/Documents/Code/GameDev/Drawing Game" && pnpm --filter @crayon-world/server test 2>&1 | tail -20</automated>
  </verify>
  <done>Full server test suite passes (both recognize and interactions tests). Mock mode curl returns valid InteractionMatrix JSON. Edge cases return expected responses (400 for empty, empty entries for single entity).</done>
</task>

</tasks>

<verification>
- `pnpm --filter @crayon-world/server test` passes all tests (recognize + interactions)
- POST /api/interactions with mock mode returns valid InteractionMatrix
- POST /api/interactions with empty/missing entities returns 400
- POST /api/interactions with 1 entity returns { entries: [] }
- Route is registered in server/src/index.ts
</verification>

<success_criteria>
- POST /api/interactions accepts entity profiles array and returns InteractionMatrix (BATC-01)
- Response classifies each entity pair with one of 5 interaction types, asymmetrically (BATC-02)
- Mock mode returns canned matrix without Anthropic API call (BATC-03)
- All server tests pass with no regressions
- Route independently testable with curl in mock mode
</success_criteria>

<output>
After completion, create `.planning/phases/06-shared-types-interaction-server-route/06-02-SUMMARY.md`
</output>
