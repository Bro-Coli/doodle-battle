---
phase: 06-react-control-surface
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - client/src/main.ts
  - client/src/style.css
  - client/src/ui/StudioControlsApp.tsx
  - client/src/ui/Toolbar.tsx
  - client/src/ui/ActionButtons.tsx
  - client/src/ui/ThicknessToggle.tsx
  - client/src/ui/MockModeBadge.tsx
autonomous: false
requirements: [UIM-01, UIM-03, UIM-04]

must_haves:
  truths:
    - "Submit, Undo, Clear, and the Draw/World mode toggle render through React components"
    - "Brush thickness controls render through React and continue to drive DrawingCanvas thickness changes"
    - "World mode keeps the toggle enabled while draw-specific controls disable"
    - "The compact top-centered toolbar layout is preserved"
    - "Mock-mode status can render with the React-owned control surface without pulling in Phase 7 overlays"
  artifacts:
    - path: "client/src/ui/Toolbar.tsx"
      provides: "React toolbar layout and grouping"
      contains: "Toolbar"
    - path: "client/src/ui/ThicknessToggle.tsx"
      provides: "React brush thickness controls"
      contains: "ThicknessToggle"
    - path: "client/src/style.css"
      provides: "Toolbar and control-surface styles compatible with React markup"
      contains: "#toolbar"
  key_links:
    - from: "client/src/ui/Toolbar.tsx"
      to: "client/src/ui/createStudioController.ts"
      via: "consumes controller state and actions"
      pattern: "controller"
    - from: "client/src/style.css"
      to: "client/src/ui/Toolbar.tsx"
      via: "preserves compact top-centered toolbar presentation"
      pattern: "toolbar"
---

<objective>
Complete the visible Phase 6 migration by replacing the imperative toolbar DOM with React-rendered controls and verifying that draw/world interaction parity still holds in the browser.

Purpose: After Plan 01 establishes the React mount and controller bridge, this plan moves the actual control UI into React components and validates that the player workflow remains unchanged.

Output: React-rendered toolbar components, updated styling, and browser-verified parity for mode switching, button disable rules, brush thickness changes, and mock-mode status display.
</objective>

<execution_context>
@/Users/sumin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sumin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-react-control-surface/06-CONTEXT.md
@.planning/phases/06-react-control-surface/06-RESEARCH.md
@.planning/phases/06-react-control-surface/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the imperative toolbar with React components</name>
  <read_first>
    - client/src/main.ts
    - client/src/style.css
    - client/src/ui/StudioControlsApp.tsx
    - client/src/ui/createStudioController.ts
    - .planning/phases/06-react-control-surface/06-CONTEXT.md
  </read_first>
  <files>client/src/main.ts, client/src/style.css, client/src/ui/StudioControlsApp.tsx, client/src/ui/Toolbar.tsx, client/src/ui/ActionButtons.tsx, client/src/ui/ThicknessToggle.tsx, client/src/ui/MockModeBadge.tsx</files>
  <acceptance_criteria>
    - The visible toolbar controls render through React components rather than DOM nodes built in `main.ts`
    - The top-centered compact layout and current grouping remain recognizable after the migration
    - Submit, Clear, Undo, and thickness controls reflect the same enabled/disabled rules as before
    - The Draw/World toggle remains always available and updates its label based on mode
  </acceptance_criteria>
  <action>
1. Create a small React component split for the control surface, for example:
   - `Toolbar.tsx` for layout
   - `ActionButtons.tsx` for submit/undo/clear/view toggle
   - `ThicknessToggle.tsx` for brush presets
   - `MockModeBadge.tsx` for the lightweight status badge if it is moved in this phase
2. Update `StudioControlsApp.tsx` to render these components from controller state.
3. Remove the old imperative toolbar creation and append logic from `client/src/main.ts`. The file should still create the Pixi canvas and React mount, but the toolbar itself should now be React-owned.
4. Update `client/src/style.css` to style the React markup while preserving the established visual presentation. Avoid redesigning the experience.
5. Do not migrate spinner/result/error overlays here. If any overlay code in `RecognitionOverlay` still references toolbar-era assumptions, adapt only what is necessary for Phase 6 parity.
  </action>
  <verify>
    <automated>pnpm --filter @crayon-world/client build</automated>
  </verify>
  <done>All visible Phase 6 controls are React-rendered, `main.ts` no longer imperatively assembles the toolbar, and the existing control layout/behavior remains intact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify draw/world parity and brush controls in the browser</name>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/06-react-control-surface/06-CONTEXT.md
    - client/src/main.ts
    - client/src/ui/StudioControlsApp.tsx
    - client/src/style.css
  </read_first>
  <files>client/src/main.ts, client/src/ui/StudioControlsApp.tsx, client/src/style.css</files>
  <acceptance_criteria>
    - Submit, Undo, Clear, and Draw/World controls are visibly React-rendered and behave correctly
    - Changing thickness still affects the next strokes on the drawing surface
    - Switching between draw mode and world mode does not break stage visibility or control-state rules
    - The client entrypoint no longer creates the toolbar directly through `document.createElement`
  </acceptance_criteria>
  <action>
Run the app and verify Phase 6 behavior in a browser.

1. Start the client and server using the project’s normal dev commands.
2. Open the app and confirm the toolbar is present and React-rendered.
3. In draw mode:
   - draw at least one stroke
   - verify Submit, Clear, and Undo enable when the canvas is non-empty
   - verify Undo removes the last stroke
   - verify Clear empties the canvas
   - verify Thin, Medium, and Thick affect subsequent strokes
4. Switch to world mode:
   - confirm the toggle label changes appropriately
   - confirm draw-specific controls disable while the mode toggle stays usable
   - confirm switching back restores correct control state
5. If mock mode is active, confirm the lightweight status badge still appears in the React-owned surface (or remains intentionally deferred with no regression if left outside the toolbar).
6. Inspect `client/src/main.ts` and confirm there is no direct imperative toolbar assembly remaining.
  </action>
  <verify>Human browser verification plus source inspection</verify>
  <done>Phase 6 success criteria are verified: the control surface is React-rendered, brush controls still drive drawing behavior, draw/world mode parity holds, and the toolbar is no longer created imperatively in `main.ts`.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @crayon-world/client build` passes
- Browser verification confirms draw-mode actions, thickness presets, and draw/world toggle parity
- `client/src/main.ts` no longer contains imperative toolbar creation code
- Lightweight status UI remains coherent with the React control surface while Phase 7 overlays stay deferred
</verification>

<success_criteria>
- Submit, Undo, Clear, and draw/world mode toggle are React-rendered
- Brush thickness controls are React-rendered and still affect drawing
- Draw mode and world mode continue to work without visibility or interaction regressions
- The toolbar is no longer created in `main.ts` via imperative DOM assembly
</success_criteria>

<output>
After completion, create `.planning/phases/06-react-control-surface/06-02-SUMMARY.md`
</output>
