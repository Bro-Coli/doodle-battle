---
phase: 06-react-control-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/package.json
  - client/tsconfig.json
  - client/vite.config.ts
  - pnpm-lock.yaml
  - client/src/main.ts
  - client/src/ui/createStudioController.ts
  - client/src/ui/types.ts
  - client/src/ui/StudioControlsApp.tsx
autonomous: true
requirements: [UIM-01, UIM-03, UIM-04]

must_haves:
  truths:
    - "The client has a React mount point separate from the Pixi canvas"
    - "A controller boundary bridges React UI actions to DrawingCanvas, WorldStage, and RecognitionOverlay"
    - "Toolbar state includes canvas emptiness, world mode, selected thickness, submit busy state, and mock mode"
    - "Keyboard undo still routes through the same undo action after the migration"
    - "main.ts no longer assembles the toolbar through imperative button creation"
  artifacts:
    - path: "client/src/ui/createStudioController.ts"
      provides: "React-facing controller for toolbar state and actions"
      contains: "createStudioController"
    - path: "client/src/ui/StudioControlsApp.tsx"
      provides: "React app root for the control surface"
      contains: "StudioControlsApp"
    - path: "client/src/main.ts"
      provides: "Pixi bootstrap plus React mount and controller wiring"
      contains: "createRoot"
  key_links:
    - from: "client/src/main.ts"
      to: "client/src/ui/createStudioController.ts"
      via: "runtime objects are wrapped in a controller passed to React"
      pattern: "createStudioController"
    - from: "client/src/ui/StudioControlsApp.tsx"
      to: "client/src/ui/types.ts"
      via: "shared UI/controller contract"
      pattern: "StudioControls"
---

<objective>
Establish the React runtime boundary for the Phase 6 control-surface migration without changing the underlying draw/world/recognition engine behavior.

Purpose: Phase 6 needs React to own the visible toolbar, but the existing Pixi runtime and recognition overlay logic are already working. This plan introduces the minimum React bootstrap and controller abstraction required to migrate the controls safely.

Output: React-enabled client build setup, a React mount point, and a controller module that exposes toolbar state/actions while preserving current runtime behavior.
</objective>

<execution_context>
@/Users/sumin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sumin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-react-control-surface/06-CONTEXT.md
@.planning/phases/06-react-control-surface/06-RESEARCH.md

<interfaces>
From `client/src/drawing/DrawingCanvas.ts`:
```ts
undo(): void;
clear(): void;
setThickness(preset: ThicknessPreset): void;
get isEmpty(): boolean;
get undoStack(): UndoStack;
```

From `client/src/world/WorldStage.ts`:
```ts
toggle(): void;
get inWorld(): boolean;
spawnEntity(app: Application, strokeContainer: Container, profile: EntityProfile): void;
```

From `client/src/recognition/RecognitionOverlay.ts`:
```ts
showSpinner(): void;
hideSpinner(): void;
showCard(profile: EntityProfile, onDismiss: () => void): void;
showError(message: string, onDismiss: () => void, onRetry?: () => void): void;
showMockBadge(): void;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the React client bootstrap and shared UI contract</name>
  <read_first>
    - client/package.json
    - client/tsconfig.json
    - client/vite.config.ts
    - client/src/main.ts
    - .planning/phases/06-react-control-surface/06-CONTEXT.md
    - .planning/phases/06-react-control-surface/06-RESEARCH.md
  </read_first>
  <files>client/package.json, client/tsconfig.json, client/vite.config.ts, pnpm-lock.yaml, client/src/ui/types.ts, client/src/ui/StudioControlsApp.tsx</files>
  <acceptance_criteria>
    - The client package includes the React dependencies needed to render a React surface through Vite
    - The Vite and TypeScript config support `.tsx` files without breaking the current client build
    - A React root component exists for the studio control surface and consumes a typed controller contract rather than directly instantiating Pixi objects
  </acceptance_criteria>
  <action>
1. Update `client/package.json` to add `react`, `react-dom`, and any required Vite React integration package. Update `pnpm-lock.yaml` accordingly.
2. Update `client/tsconfig.json` and `client/vite.config.ts` for a minimal React + TSX setup that remains compatible with the current client package structure.
3. Create `client/src/ui/types.ts` to define the narrow controller contract React will consume. Include state for canvas emptiness, world mode, selected thickness, submit busy state, and mock mode, plus actions for submit, undo, clear, view toggle, and thickness selection.
4. Create `client/src/ui/StudioControlsApp.tsx` as the React root for Phase 6. Keep it minimal in this plan: it should render from the typed controller contract and be structured to support the full toolbar migration in Plan 02.
5. Do not move recognition loading/result/error overlays into React here. Those remain imperative and deferred to Phase 7.
  </action>
  <verify>
    <automated>pnpm --filter @crayon-world/client build</automated>
  </verify>
  <done>React build support is present, TSX files compile, and the control-surface root component can be mounted without yet changing visible toolbar behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Build the studio controller and wire React into the existing runtime</name>
  <read_first>
    - client/src/main.ts
    - client/src/drawing/DrawingCanvas.ts
    - client/src/world/WorldStage.ts
    - client/src/recognition/RecognitionOverlay.ts
    - client/src/ui/types.ts
    - client/src/ui/StudioControlsApp.tsx
  </read_first>
  <files>client/src/main.ts, client/src/ui/createStudioController.ts, client/src/ui/StudioControlsApp.tsx</files>
  <acceptance_criteria>
    - `main.ts` mounts React into a dedicated DOM node and no longer creates the toolbar buttons directly with `document.createElement`
    - The controller encapsulates the existing toolbar action logic, including submit flow disabling, world-mode disable rules, and mock-mode detection
    - Keyboard undo calls the same controller action path used by the React toolbar
  </acceptance_criteria>
  <action>
1. Create `client/src/ui/createStudioController.ts` to wrap the existing runtime objects (`DrawingCanvas`, `WorldStage`, `RecognitionOverlay`, Pixi `Application`) behind the typed controller contract.
2. Move the current submit/clear/undo/view-toggle/thickness/mock-mode logic out of the imperative toolbar block in `client/src/main.ts` and into the controller boundary.
3. Refactor `client/src/main.ts` so it:
   - boots Pixi as today
   - creates the runtime objects as today
   - creates a React mount node
   - mounts `StudioControlsApp`
   - keeps the keyboard undo listener, but routes it through the shared controller action
4. Preserve the existing draw/world behavior and recognition-overlay behavior exactly. The goal of this plan is ownership transfer, not UX redesign.
5. Leave the actual toolbar markup/styling migration for Plan 02, but ensure the React mount path and controller contract are stable enough for that plan to complete without reworking the runtime again.
  </action>
  <verify>
    <automated>pnpm --filter @crayon-world/client build</automated>
  </verify>
  <done>The client boots through the same Pixi runtime plus a React UI mount, and the control-surface state/actions are available to React through a dedicated controller module.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @crayon-world/client build` passes after the React bootstrap changes
- `client/src/main.ts` no longer creates toolbar buttons via direct imperative DOM assembly
- React is mounted through a dedicated UI root while Pixi canvas boot remains intact
- The controller exposes state/action hooks required for submit, undo, clear, thickness, and draw/world mode
</verification>

<success_criteria>
- React is available in the client and mounted through a dedicated UI boundary
- The toolbar behavior is represented by a controller contract rather than ad hoc DOM logic in `main.ts`
- Existing runtime behavior remains the source of truth for draw/world and recognition flow
</success_criteria>

<output>
After completion, create `.planning/phases/06-react-control-surface/06-01-SUMMARY.md`
</output>
